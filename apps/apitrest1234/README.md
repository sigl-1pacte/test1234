# apitrest1234

Generated by the CNP provisioning API.

## Metadata

- Team: `valentin-team`
- Framework: `fastapi`

## Run locally

```sh
uv sync
uv run python -m app.main
```

## CNP contract

The original request is stored in `.cnp/app-request.json`.
If logging is enabled, application logs must be written to stdout/stderr so Grafana Alloy can forward Pod logs to Loki.

## CI/CD

On push to `main`, `.github/workflows/apitrest1234.yml` builds this app's image,
pushes it to GHCR, and updates its `cnp-gitops` values file so ArgoCD deploys
it — automatically. This uses an auto-provisioned `GHCR_GITOPS_TOKEN` repo
secret; you don't need to create it yourself.
