[project]
name = "apitrest1234"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.50.2",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.15.17",
    "bandit>=1.9.4",
    "pip-audit>=2.10.1",
]
